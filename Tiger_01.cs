using UnityEngine;
using System.Collections;

public class Tiger_01 : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	//	Vector3 p = Camera.main.ScreenToWorldPoint(Input.mousePosition);
		
		//Debug.Log(p);
		
		//transform.position = Vector3.MoveTowards(transform.position, new Vector3(p.x,p.y,-1.0f), 1.0f);
	//	transform.position = Vector3.Lerp(transform.position,new Vector3(p.x,p.y,-1.0f),Time.deltaTime);
		//Debug.Log (transform.position + "tiger");
			
	}




}
